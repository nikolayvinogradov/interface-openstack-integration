site_name: 'OpenStack Integration Interface'

generate:
  - requires.md:
    - requires
    - requires.OpenStackRequires+
  - provides.md:
    - provides
    - provides.OpenStackProvides+
    - provides.IntegrationRequest+

pages:
  - Requires: requires.md
  - Provides: provides.md

gens_dir: docs
